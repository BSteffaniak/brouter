[package]
authors     = { workspace = true }
categories  = ["config"]
description = "Fallback model catalog data models for brouter"
edition     = { workspace = true }
keywords    = ["catalog", "llm", "models"]
license     = { workspace = true }
name        = "brouter_catalog_models"
readme      = "README.md"
repository  = { workspace = true }
version     = { workspace = true }

[dependencies]
serde = { workspace = true, features = ["derive", "std"] }

[features]
default          = []
fail-on-warnings = []
